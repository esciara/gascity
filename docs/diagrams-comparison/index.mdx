---
title: Diagrams Comparison
description: Side-by-side comparison of the same diagrams rendered with Mermaid (ELK), D2, and Excalidraw.
---

This page renders the same four diagrams in three different tools so they can be compared visually.

- **Mermaid** is rendered inline by Mintlify's `mermaid` code block, with the ELK renderer enabled via the `%%{init}%%` directive on flowcharts.
- **D2** sources live under `docs/diagrams-comparison/d2/` and are compiled to SVG with `make diagrams-d2` (uses `d2 --layout=elk`). Compiled SVGs are committed under `docs/diagrams-comparison/d2-rendered/`.
- **Excalidraw** sources live under `docs/diagrams-comparison/excalidraw/` and are rendered to SVG with `make diagrams-excalidraw` (uses `@swiftlysingh/excalidraw-cli`). Compiled SVGs are committed under `docs/diagrams-comparison/excalidraw-rendered/`.

Run `make diagrams` to (re-)render everything.

## A. Simple flowchart — user login

_To be populated in Phase 2._

### Mermaid

### D2

### Excalidraw

## B. System architecture — small web app

_To be populated in Phase 2._

### Mermaid

### D2

### D2 (sketch mode)

### Excalidraw

## C. Sequence diagram — OAuth 2.0 authorization code

_To be populated in Phase 2._

### Mermaid

### D2

### Excalidraw

## D. ER-style data model — User / Order / Product

_To be populated in Phase 2._

### Mermaid

### D2

### Excalidraw

## Summary

_To be populated in Phase 2._
