---
title: Diagrams Comparison
description: Side-by-side comparison of the same diagrams rendered with Mermaid, D2, and Excalidraw.
---

This page renders the same four diagrams in three different tools so they can be compared visually.

- **Mermaid** is rendered inline by Mintlify's `mermaid` code block.
- **D2** sources live under `docs/diagrams-comparison/d2/` and are compiled to SVG with `make diagrams-d2`. Compiled SVGs are committed under `docs/diagrams-comparison/d2-rendered/`.
- **Excalidraw** sources live under `docs/diagrams-comparison/excalidraw/` and are rendered to SVG with `make diagrams-excalidraw` (uses `@swiftlysingh/excalidraw-cli`). Compiled SVGs are committed under `docs/diagrams-comparison/excalidraw-rendered/`.

Run `make diagrams` to (re-)render everything.

## A. Simple flowchart — user login

Tests basic shapes, branching, and labels.

### Mermaid

```mermaid
flowchart TD
  start([Start]) --> creds[Enter credentials]
  creds --> validate{Validate?}
  validate -- success --> dashboard[Dashboard]
  validate -- failure --> error[Show error]
  error -- retry --> creds

  style start fill:#a5d8ff,stroke:#1e1e1e
  style creds fill:#ffd8a8,stroke:#1e1e1e
  style validate fill:#fff3bf,stroke:#1e1e1e
  style dashboard fill:#b2f2bb,stroke:#1e1e1e
  style error fill:#ffc9c9,stroke:#1e1e1e

  linkStyle 2 stroke:#22c55e
  linkStyle 3 stroke:#ef4444
  linkStyle 4 stroke:#757575
```

### D2

![D2 — login flow](d2-rendered/a-login-flow.svg)

### Excalidraw

![Excalidraw — login flow](excalidraw-rendered/a-login-flow.svg)

## B. System architecture — small web app

Tests containers/grouping and dense layouts: browser → CDN → load balancer → two app server instances → primary DB + read replica + Redis cache. App servers and data layer are grouped.

### Mermaid

```mermaid
flowchart LR
  browser([Browser]) --> cdn[CDN]
  cdn --> lb[Load Balancer]
  subgraph apps[App Servers]
    app1[App Server 1]
    app2[App Server 2]
  end
  lb --> app1
  lb --> app2
  subgraph data[Data Layer]
    db[(Primary DB)]
    replica[(Read Replica)]
    cache[(Redis)]
  end
  app1 --> db
  app1 --> replica
  app1 --> cache
  app2 --> db
  app2 --> replica
  app2 --> cache
  db -. replication .-> replica

  style browser fill:#a5d8ff,stroke:#1e1e1e
  style cdn fill:#fff3bf,stroke:#1e1e1e
  style lb fill:#ffd8a8,stroke:#1e1e1e
  style app1 fill:#d0bfff,stroke:#1e1e1e
  style app2 fill:#d0bfff,stroke:#1e1e1e
  style db fill:#b2f2bb,stroke:#1e1e1e
  style replica fill:#c3fae8,stroke:#1e1e1e
  style cache fill:#ffc9c9,stroke:#1e1e1e
  linkStyle 8 stroke:#22c55e
```

### D2

![D2 — architecture](d2-rendered/b-architecture.svg)

### D2 (sketch mode)

![D2 sketch — architecture](d2-rendered/b-architecture-sketch.svg)

### Excalidraw

![Excalidraw — architecture](excalidraw-rendered/b-architecture.svg)

## C. Sequence diagram — OAuth 2.0 authorization code

Tests how each tool handles sequence/interaction diagrams. Mermaid and D2 both support sequence diagrams natively. Excalidraw has no native sequence syntax, so the version below is a hand-built swim-lane approximation (4 actors with vertical lifelines and labeled message arrows).

### Mermaid

```mermaid
sequenceDiagram
  box rgb(165, 216, 255) User
    participant U as User
  end
  box rgb(208, 191, 255) Client App
    participant C as Client App
  end
  box rgb(255, 216, 168) Auth Server
    participant A as Auth Server
  end
  box rgb(178, 242, 187) Resource Server
    participant R as Resource Server
  end
  U->>C: 1. Click "Log in"
  C->>A: 2. Redirect to /authorize
  A->>U: 3. Show login page
  U->>A: 4. Submit credentials + consent
  A->>C: 5. Redirect with auth code
  C->>A: 6. POST /token (code + secret)
  A-->>C: 7. Access token (+ refresh)
  C->>R: 8. GET /api with token
  R-->>C: 9. Return protected resource
```

### D2

![D2 — OAuth sequence](d2-rendered/c-oauth-sequence.svg)

### Excalidraw

![Excalidraw — OAuth sequence (swim-lane)](excalidraw-rendered/c-oauth-sequence.svg)

## D. ER-style data model — User / Order / Product

Tests how each tool handles structured data shapes: three entities with fields and relationships.

### Mermaid

```mermaid
erDiagram
  USER ||--o{ ORDER : places
  ORDER }|--|{ PRODUCT : contains
  USER {
    int id PK
    string email
    string name
    datetime created_at
  }
  ORDER {
    int id PK
    int user_id FK
    decimal total
    string status
    datetime placed_at
  }
  PRODUCT {
    int id PK
    string sku
    string name
    decimal price
  }
```

### D2

![D2 — ER model](d2-rendered/d-er-model.svg)

### Excalidraw

Excalidraw has no native ER table shape — the version below is a hand-built approximation using a colored header band over a list of text rows. The PK/FK markers are inline labels.

![Excalidraw — ER model](excalidraw-rendered/d-er-model.svg)

## Summary

Numbers for **diagram B** (architecture diagram — the densest of the four):

| Tool | Source lines (B) | Build step? | Rendered output size (B) |
| --- | --- | --- | --- |
| Mermaid | 22 (inline in this .mdx) | No — rendered live by Mintlify | n/a (inline SVG, no committed file) |
| D2 | 30 | Yes — `make diagrams-d2` | 31 KB SVG |
| D2 (sketch mode) | 36 | Yes — `make diagrams-d2` | 126 KB SVG |
| Excalidraw | 478 | Yes — `make diagrams-excalidraw` | 28 KB SVG |
